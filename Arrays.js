//CRIANDO LISTAS COM ARRAYS

//exemplo 1
const nota1 = 6;
const nota2 = 8;
const nota3 = 5;
const nota4 = 7;

const operacaoMatematica = (nota1 + nota2 + nota3 + nota4) / 4;

console.log(operacaoMatematica);




//fazer a mesma operação usando listas (Array).
const notas = [6, 8, 5, 7];

const mediaNotas = (notas[0] + notas[1] + notas[2] + notas[3]) / notas.length;

console.log(mediaNotas);










