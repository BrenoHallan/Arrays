//ADICIONANDO ELEMENTOS A UMA LISTA

const valores = [230, 256, 361, 567]

valores.push(562); // "push" adiciona um elemento no final da lista

const media = (valores[0] + valores[1] + valores[2] + valores[3] + valores[4]) / valores.length;

console.log(media);

















